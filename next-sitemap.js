/** @type {import('next-sitemap').IConfig} */

module.exports = {
    siteUrl: process.env.SITE_URL || 'https://csepulveda.io',
    generateRobotsTxt: true, // (optional)
     // Default transformation function
  }